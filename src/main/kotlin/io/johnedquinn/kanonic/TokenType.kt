package io.johnedquinn.kanonic

enum class TokenType {
    BRACKET_LEFT,
    BRACKET_RIGHT,
    EPSILON,
    EQUALS,
    IDENTIFIER,
    INTEGER,
    MINUS,
    PAREN_LEFT,
    PAREN_RIGHT,
    PLUS,
    STRING,
    EOF
}