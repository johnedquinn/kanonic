package io.johnedquinn.kanonic

enum class TokenType {
    ASTERISK,
    BRACKET_LEFT,
    BRACKET_RIGHT,
    EPSILON,
    EQUALS,
    HASHTAG,
    IDENTIFIER,
    INTEGER,
    MINUS,
    PAREN_LEFT,
    PAREN_RIGHT,
    PLUS,
    STRING,
    EOF
}